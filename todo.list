TODO:

Component Block:
    fix saving state while modification
    add parameter field
    add Arduino/Python compatibility
    Make parameters show up in toolbox
    ================change parameter to input       DONE
    ================enable blocks in NONE mode      DONE
    ================remove deletability             DONE
    ================make it show up by default      DONE

Previously Created Blocks:
    ================Disabled duplicating blocks     DONE
    ================fix Deleting blocks             DONE
    ================let blocks be named             DONE
    ================find a good way to make         DONE
                    connections between blocks
                    on the workspace and their
                    respective outputs
    ================Fix problem where are           DONE
                    blocks with the same name
                    are linked
    ================Don't display category for      DONE
                    blocks without outputs
    ================Block '|' and '\' from          DONE
                    being in names

    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    FORMAT:
    Initial naming: Blocly.Blocks["Len|0"]
    Initial Ouput name: Blocly.Blocks["Len|0\0"], Blocly.Blocks["Len|0\1"], Blocly.Blocks["Len|0\2"]
    default initial nameField, category name: block name0

    Once a  block is dragged to workspace,
        create new block, Blocly.Blocks["Len|i+1"]
        create new output block Initial Ouput name: Blocly.Blocks["Len|i+1\0"], Blocly.Blocks["Len|i+1\1"], Blocly.Blocks["Len|i+1\2"]
        nameField, category name:  block namei+1
    ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

Low Priority:
    make primitives show up automatically rather than on 127.0.0.1:8000/interface
